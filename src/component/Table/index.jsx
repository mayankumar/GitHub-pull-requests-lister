// @flow
import * as React from 'react'

export const Table = (props) => {
  const { pullRequests } = props
  console.log(pullRequests, 'in table')
  return (
    <div>
      <div> We are here</div>
    </div>
  )
}
